export const handler = async (event) => {

  //var msg = event.queryStringParameters.key1;
  // TODO implement
  const response = {
    statusCode: 200,
    body: JSON.stringify("abc"),
  };
  return response;
};